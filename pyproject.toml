[project]
name = "gitoy-2"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "colored>=2.3.0",
    "fire>=0.7.0",
]

[project.scripts]
gitoy = "cli:main"
